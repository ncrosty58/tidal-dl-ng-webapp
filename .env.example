# Copy to .env (gitignored) and adjust as needed.

# Leave empty for LAN-only use (the built-in UI has no way to send this
# header, so setting it would lock out the web UI itself). Only set it if
# you put this behind a reverse proxy that injects the header, or extend
# the frontend to prompt for it.
DOWNLOAD_TOKEN=
